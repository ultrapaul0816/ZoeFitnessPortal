# Local Development Environment
# Copy this file to .env.local (or .env) and fill in values.
#
# SWITCHING BETWEEN LOCAL AND PROD:
#   Local DB:  Set DATABASE_URL to your local PostgreSQL instance
#   Prod DB:   Set DATABASE_URL to the Neon PostgreSQL connection string
#   The app reads DATABASE_URL — just swap the value to switch.
#   NEVER run seed scripts against prod!

# Local PostgreSQL Database
# Option A: Local PostgreSQL (brew install postgresql@16 && brew services start postgresql@16)
DATABASE_URL=postgresql://localhost:5432/zoe_fitness_local
# Option B: Neon dev branch (create at https://console.neon.tech)
# DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Session secret (any random string for local dev)
SESSION_SECRET=local-dev-secret-change-me

# AI Features (Anthropic Claude preferred, OpenAI as fallback)
ANTHROPIC_API_KEY=sk-ant-api03-PLACEHOLDER
# OPENAI_API_KEY=sk-your-openai-key-here

# Email (optional for local dev — emails will log to console if not set)
# RESEND_API_KEY=re_your_key_here
